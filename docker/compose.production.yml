name: next-shadcn

services:
  web:
    build:
      context: ..
      dockerfile: app/Dockerfile
      target: production
    restart: unless-stopped
    ports:
      - 3000:3000
    env_file:
      - ../app/.env.production
